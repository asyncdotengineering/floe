/**
 * defineInbox — the single public entrypoint of @floe/inbox.
 *
 * Wraps an `@floe/runtime` Assistant with the inbox-layer primitives:
 * tenant resolution, Turn lifecycle, Confidence scoring, Handoff policy,
 * Workflow hooks, ConversationStore, outcomes telemetry, and the
 * OpenAI-SSE canonical wire shape (with floe.* events alongside).
 *
 * Current status: skeleton (C-1). Real entrypoint lands in C-14.
 */

export {};
